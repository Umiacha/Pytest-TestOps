[project]
name = "demo-algorithms"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.0.0",
    "pytest-testops",
]

[tool.uv.sources]
pytest-testops = { workspace = true }

[build-system]
requires = ["uv_build>=0.11.26,<0.12"]
build-backend = "uv_build"